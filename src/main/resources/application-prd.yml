spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
#    url: jdbc:postgresql://junction.proxy.rlwy.net:42399/railway
    username: ${PGUSER}
    password: ${PGPASSWORD}
#    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQL82Dialect

